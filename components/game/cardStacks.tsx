import React, { FunctionComponent } from 'react'
import { Box } from '@chakra-ui/react'
import { CardDuno } from 'types'
import { AppButton } from 'components'

type Props = {}

export const CardStacks: FunctionComponent<Props> = ({}) => {
  return <Box></Box>
}
